# CRM Atlética FGV Rio — dependências de produção
Django>=5.1,<5.3
django-environ>=0.11
psycopg[binary]>=3.1
whitenoise>=6.6
Brotli>=1.1          # WhiteNoise gera .br além de .gz no collectstatic
django-simple-history>=3.5
Pillow>=10.2
gunicorn>=22.0

# API JSON consumida pelo frontend React (frontend/)
djangorestframework>=3.15
django-filter>=24.0

# Integração Google Sheets (fase 2)
gspread>=6.1
google-auth>=2.29
